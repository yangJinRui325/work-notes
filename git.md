## git命令
```markdown
//初始化一个git仓库
git init

//设置当前仓库的用户名和email地址
git config --global user.name "Your Name"
git config --global user.emial "emial@example.com

//查看仓库的当前状态(增删改提等)
git status

//查看difference
git diff




```